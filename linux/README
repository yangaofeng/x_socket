uuid 演示linux下uuid相关函数的用法
backtrace 演示打印函数调用栈

eventfd 演示linux新型api eventfd的用法，eventfd可以取代pipe用来做事件通知，将事件通知统一到select,epoll等io多路复用模型中。
signalfd 演示linux新型api signalfd的用法，signalfd和eventfd类似，用来做信号通知，可以将信号处理统一到select,epoll等io多路复用模型中。 
timerfd 演示linux新型api timerfd的用法，timerfd和eventfd类似，用来做定时器通知，可以将定时器处理统一到select,epoll等io多路复用模型中。 

process目录下演示和进程相关的api调用,如守护进程，进程组，会话的api调用。

thread目录下演示和线程相关的api调用,如线程本地存储

so 目录下演示动态库的编写和动态库的编译,这个示例来自qlog项目，dynamicappender加载的动态库测试代码，可以使用dlopen,dlsym加载其中的函数

so_soname 演示soname的用法，解析了主版本号，次版本号，debug版本号的用法,url:http://www.cnblogs.com/zhyg6516/archive/2011/01/01/1923789.html

a 演示静态库的生成和使用,示例来自程序员的自称修养--链接、装载和库

